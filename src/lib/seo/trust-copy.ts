/**
 * Accurate trust & product claims — single source for FAQ and marketing copy.
 * Only describe features that exist today (no accounts, no saved scan history).
 */

/** Resume file handling — must match /privacy policy. */
export const RESUME_STORAGE_FAQ_ANSWER =
  "We process your file to generate scores and feedback for that request. We do not persist the original file or extracted text in a long-term database. We do not sell resume data or use it to train unrelated models. See our privacy policy for details.";

/** No auth / accounts on ResumeIQ today. */
export const NO_ACCOUNT_REQUIRED_ANSWER =
  "No. ResumeIQ does not offer user accounts. Upload or paste your resume for standard checks without signup or payment.";

/** Core product is free — no paid tier or upgrade path today. */
export const CORE_CHECKER_FREE_ANSWER =
  "Yes. Core ATS score, parser preview, keyword gaps, format flags, and improvement checklist stay free with no signup and no paywall on results.";

/** No payment required for core checker. */
export const NO_PAYMENT_FOR_CORE_CHECKER_ANSWER =
  "No. ResumeIQ keeps core ATS checks free—score, parser preview, keyword gaps, and format flags—with no signup and no paid tier required for standard use.";

/** Transparent editorial attribution — no individual fake bylines. */
export const EDITORIAL_TEAM_NAME = "ResumeIQ Editorial Team";

export const EDITORIAL_TEAM_DESCRIPTION =
  "The collective editorial team behind ResumeIQ guides, FAQs, and tool documentation. We test advice against real PDF and DOCX parser output and published scoring methodology—not generic copied templates.";

export const EDITORIAL_TEAM_SHORT =
  "ResumeIQ Editorial Team — guides and tool docs reviewed against parser tests and published methodology.";
